/**
 * Enum that represents request HTTP request methods.
 *
 * @enum {string}
 */
enum TYPE_FETCHING {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
}

export default TYPE_FETCHING;
